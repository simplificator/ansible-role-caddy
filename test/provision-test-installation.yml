---
- hosts: "localhost"
  become: yes

  vars:
    caddy_sites:
    - domain: "example.com"
      reverse_proxy: "192.168.50.2"
      whitelist: &default_whitelist
        - "8.8.8.8/32"

  roles:
    - role: caddy
      tags: caddy
